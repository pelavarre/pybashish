# -a, --all  # list ambiguities found, not just first hit
# -s, --exit-status  # 0 if all found, else nonzero
# --python  # search python path
# --glob  # take '*' and '?' as wildcards, not as literal chars


# copied from:  git clone https://github.com/pelavarre/pybashish.git
