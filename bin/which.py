"""
usage: which.py [-h] VERB

point to the source files defining a verb here for you

positional arguments:
  VERB        the verb, such as "which.py" itself

optional arguments:
  -h, --help  show this help message and exit

examples:
  Oh no! No examples disclosed!! 💥 💔 💥
"""

# -a, --all  # list ambiguities found, not just first hit
# -s, --exit-status  # 0 if all found, else nonzero
# --python  # search python path
# --glob  # take '*' and '?' as wildcards, not as literal chars


# copied from:  git clone https://github.com/pelavarre/pybashish.git
